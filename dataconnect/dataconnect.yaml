specVersion: "v1beta"
serviceId: "chatapptute"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chatapptute-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
